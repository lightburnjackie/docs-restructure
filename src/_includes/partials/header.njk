<header>
    <!-- <nav class="navbar navbar-expand-lg bg-dark navbar-dark fixed-top"> -->
    <nav class="navbar navbar-expand-lg bg-primary navbar-dark mb-2">
        <div class="container-fluid">
            <a href="/" class="navbar-brand">Restructure Demo</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% for link in siteLinks %}
                        <li class="nav-item"><a href="{{ link.url }}" class="nav-link">{{ link.title }}</a></li>
                    {% endfor %}
                    <li class="nav-item"><a href="{{ '/internal/' | url }}" class="nav-link">Internal Notes</a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>